#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Brüche multiplizieren]
#v(40pt)
#only("1-")[- Situation: Eine Pizza teilen 🍕]
#v(20pt)
#only("2-")[- Du hast $1/2$ einer Pizza und möchtest sie gleichmäßig mit einem Freund teilen. Wie viel bekommt jeder? 🤔]
#only("1")[#voiceover("Beginnen wir mit einer Situation. Stell dir vor, du hast die Hälfte einer Pizza und möchtest sie gleichmäßig mit einem Freund teilen. Wie viel bekommt jeder von euch?")]
#only("2")[#voiceover("Um das herauszufinden, müssen wir Brüche multiplizieren. In diesem Fall müssen wir ein halb mal ein halb multiplizieren.")]
]


#slide()[
#text(size: 30pt, weight: "bold")[Brüche multiplizieren]
#v(40pt)
#only("1-")[- Schritt 1: Multipliziere die Zähler miteinander]
#v(20pt)
#only("2-")[- Schritt 2: Multipliziere die Nenner miteinander]
#v(20pt)
#only("3-")[- Beispiel: $1/2 dot 1/2 = 1/4$]
#only("1")[#voiceover("Um Brüche zu multiplizieren, folgen wir zwei einfachen Schritten. Zuerst multiplizieren wir die Zähler miteinander.")]
#only("2")[#voiceover("Zweitens multiplizieren wir die Nenner miteinander.")]
#only("3")[#voiceover("Zum Beispiel ergibt ein halb mal ein halb ein viertel. Das bedeutet, jeder von euch bekommt ein Viertel der Pizza.")]
]


#slide()[
#text(size: 30pt, weight: "bold")[Zusammenfassung]
#v(40pt)
#only("1-")[- Multipliziere die Zähler miteinander]
#v(20pt)
#only("2-")[- Multipliziere die Nenner miteinander]
#v(20pt)
#only("3-")[- Beispiel: $1/2 dot 1/2 = 1/4$]
#only("1")[#voiceover("Zusammenfassend: Beim Multiplizieren von Brüchen multiplizierst du die Zähler miteinander.")]
#only("2")[#voiceover("Dann multiplizierst du die Nenner miteinander.")]
#only("3")[#voiceover("Zum Beispiel ergibt ein halb mal ein halb ein viertel. Also bekommt jeder von euch ein Viertel der Pizza.")]
]