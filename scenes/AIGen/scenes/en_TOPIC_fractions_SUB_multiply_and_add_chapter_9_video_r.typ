#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Review: Subtracting Fractions 🧮]
#v(40pt)
#only("1-")[- Fractions represent parts of a whole 🍕]
#v(20pt)
#only("2-")[- Subtraction is removing one amount from another ➖]
#v(20pt)
#only("3-")[- Let's review how to subtract fractions 🔍]

#only("1")[
#voiceover("Let's review the process of subtracting fractions. Fractions are a way to represent parts of a whole.")
]
#only("2")[
#voiceover("When we subtract fractions, we're essentially removing one fractional amount from another.")
]
#only("3")[
#voiceover("Let's go through the steps to subtract fractions correctly.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Steps to Subtract Fractions]
#v(40pt)
#only("1-")[1️⃣ Find a common denominator]
#v(20pt)
#only("2-")[2️⃣ Convert fractions to equivalent fractions]
#v(20pt)
#only("3-")[3️⃣ Subtract the numerators]
#v(20pt)
#only("4-")[4️⃣ Simplify if possible]

#only("1")[
#voiceover("The first step in subtracting fractions is to find a common denominator. This is crucial because we can only subtract fractions when they have the same denominator.")
]
#only("2")[
#voiceover("Next, we convert each fraction to an equivalent fraction with this common denominator.")
]
#only("3")[
#voiceover("Once we have fractions with the same denominator, we can subtract the numerators while keeping the denominator the same.")
]
#only("4")[
#voiceover("Finally, we simplify the result if possible, reducing the fraction to its lowest terms.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: $3/4 - 1/2$]
#v(40pt)
#only("1-")[1️⃣ Common denominator: 4]
#v(20pt)
#only("2-")[2️⃣ Convert: $3/4 - 2/4$]
#v(20pt)
#only("3-")[3️⃣ Subtract: $(3-2)/4 = 1/4$]
#v(20pt)
#only("4-")[4️⃣ Simplify: $1/4$ (already simplified)]

#only("1")[
#voiceover("Let's work through an example. We'll subtract one-half from three-fourths. First, we need a common denominator. The least common multiple of 4 and 2 is 4, so we'll use that.")
]
#only("2")[
#voiceover("Next, we convert one-half to an equivalent fraction with denominator 4. One-half is equal to two-fourths.")
]
#only("3")[
#voiceover("Now we can subtract. Three-fourths minus two-fourths equals one-fourth.")
]
#only("4")[
#voiceover("Our result, one-fourth, is already in its simplest form, so we're done!")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary 📝]
#v(40pt)
#only("1-")[- Common denominator is key 🔑]
#v(20pt)
#only("2-")[- Convert fractions ➡️]
#v(20pt)
#only("3-")[- Subtract numerators only ➖]
#v(20pt)
#only("4-")[- Simplify when possible ✅]

#only("1")[
#voiceover("To recap, the key to subtracting fractions is finding a common denominator.")
]
#only("2")[
#voiceover("We then convert our fractions to equivalent fractions with this common denominator.")
]
#only("3")[
#voiceover("Once we have the same denominator, we only subtract the numerators, keeping the denominator the same.")
]
#only("4")[
#voiceover("Finally, we simplify our result if possible. Remember, practice makes perfect when it comes to working with fractions!")
]
]