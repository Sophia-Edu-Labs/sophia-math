#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Review: Adding Three Single-Digit Integers 🧮]
#v(40pt)
#only("1-")[- Everyday math skill 🛒]
#only("2-")[- Useful for quick calculations 🏃‍♂️]
#only("3-")[- Let's recap the process 📝]

#only("1")[
#voiceover("Let's review adding three single-digit integers, a fundamental math skill we use every day.")
]
#only("2")[
#voiceover("This skill is particularly useful for quick mental calculations, like figuring out the total cost of a few items at a store.")
]
#only("3")[
#voiceover("Let's recap the process step by step.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[The Process 📊]
#v(40pt)
#only("1-")[1️⃣ Identify the three numbers]
#v(10pt)
#only("2-")[2️⃣ Add first two numbers]
#v(10pt)
#only("3-")[3️⃣ Add result to third number]
#v(10pt)
#only("4-")[4️⃣ Get the final sum]

#only("1")[
#voiceover("The process of adding three single-digit integers is straightforward. First, we identify our three numbers.")
]
#only("2")[
#voiceover("Next, we add the first two numbers together.")
]
#only("3")[
#voiceover("Then, we take that result and add it to the third number.")
]
#only("4")[
#voiceover("This gives us our final sum.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example 🌟]
#v(40pt)
#only("1-")[Let's add: 7 + 5 + 3]
#v(20pt)
#only("2-")[$7 + 5 = 12$]
#v(20pt)
#only("3-")[$12 + 3 = 15$]
#v(20pt)
#only("4-")[Final sum: 15]

#only("1")[
#voiceover("Let's walk through an example. Say we want to add 7, 5, and 3.")
]
#only("2")[
#voiceover("First, we add 7 and 5, which gives us 12.")
]
#only("3")[
#voiceover("Then, we add this result, 12, to our third number, 3.")
]
#only("4")[
#voiceover("This gives us our final sum of 15.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary 📝]
#v(40pt)
#only("1-")[- Simple process: add step by step 🪜]
#only("2-")[- Works for any three single-digit numbers 🔢]
#only("3-")[- Practice for quick mental math! 🧠]

#only("1")[
#voiceover("To summarize, adding three single-digit integers is a simple process where we add the numbers step by step.")
]
#only("2")[
#voiceover("This method works for any combination of three single-digit numbers.")
]
#only("3")[
#voiceover("With practice, you'll be able to do these calculations quickly in your head, which is a valuable skill for everyday life!")
]
]