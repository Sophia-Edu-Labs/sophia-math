#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Review: Evaluating Power Functions 🔋]
#v(40pt)
#only("1-")[- Power functions are of the form $f(x) = x^n$.]
#only("2-")[- They are used in various real-world applications. 🌍]
#only("3-")[- Let's review how to evaluate them step by step. 🧩]
#only("1")[
#voiceover("Let's review the concept of evaluating power functions. A power function is of the form f of x equals x to the power of n.")
]
#only("2")[
#voiceover("Power functions are used in various real-world applications, such as physics, engineering, and economics.")
]
#only("3")[
#voiceover("Let's review how to evaluate them step by step.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Evaluation 📐]
#v(40pt)
#only("1-")[- Step 1: Substitute the value of $x$.]
#only("2-")[- Step 2: Calculate $x^n$.]
#only("3-")[- Example: $f(x) = 2^3$.]
#only("4-")[- Solution: $2^3 = 8$.]
#only("1")[
#voiceover("First, let's review the steps to evaluate a power function. Step 1: Substitute the value of x into the function.")
]
#only("2")[
#voiceover("Step 2: Calculate x to the power of n.")
]
#only("3")[
#voiceover("For example, let's evaluate the function f of x equals 2 to the power of 3.")
]
#only("4")[
#voiceover("Substituting 2 for x, we get 2 to the power of 3, which equals 8.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Evaluating $f(x) = x^3$]
#v(40pt)
#only("1-")[$f(2) = 2^3$]
#only("2-")[$= 8$]
#only("3-")[$f(3) = 3^3$]
#only("4-")[$= 27$]
#only("5-")[$f(4) = 4^3$]
#only("6-")[$= 64$]
#only("1")[
#voiceover("Let's look at some examples. First, let's evaluate f of 2 for the function f of x equals x cubed.")
]
#only("2")[
#voiceover("Substituting 2 for x, we get 2 to the power of 3, which equals 8.")
]
#only("3")[
#voiceover("Next, let's evaluate f of 3 for the same function.")
]
#only("4")[
#voiceover("Substituting 3 for x, we get 3 to the power of 3, which equals 27.")
]
#only("5")[
#voiceover("Finally, let's evaluate f of 4 for the function.")
]
#only("6")[
#voiceover("Substituting 4 for x, we get 4 to the power of 3, which equals 64.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary 📝]
#v(40pt)
#only("1-")[- To evaluate power functions, substitute $x$.]
#only("2-")[- Calculate $x^n$.]
#only("3-")[- Example: $f(2) = 2^3 = 8$.]
#only("4-")[- Useful in many fields. 🌍]
#only("1")[
#voiceover("So to summarize, to evaluate power functions, we first substitute the value of x.")
]
#only("2")[
#voiceover("Then, we calculate x to the power of n.")
]
#only("3")[
#voiceover("For example, f of 2 equals 2 to the power of 3, which equals 8.")
]
#only("4")[
#voiceover("Understanding how to evaluate power functions is useful in many fields such as physics, engineering, and economics.")
]
]