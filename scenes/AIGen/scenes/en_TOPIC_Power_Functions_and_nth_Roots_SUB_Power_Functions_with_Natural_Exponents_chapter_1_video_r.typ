#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Review: Evaluating Power Functions 🔋]
#v(40pt)
#only("1-")[- Power functions are used to model various real-world situations. 🌍]
#only("2-")[- They have the form $f(x) = x^n$ where $n$ is a constant. 📈]
#only("3-")[- Let's review how to evaluate them. 🔄]
#only("1")[
#voiceover("Let's review the concept of evaluating power functions. Power functions are used to model various real-world situations.")
]
#only("2")[
#voiceover("A power function has the form f of x equals x raised to the power of n, where n is a constant.")
]
#only("3")[
#voiceover("Now, let's review how to evaluate these functions.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Evaluation 🧮]
#v(40pt)
#only("1-")[- Substitute the given value of $x$ into the function. 🔄]
#only("2-")[- Calculate $x^n$ using the given exponent. 📏]
#only("3-")[- Example: Evaluate $f(2)$ for $f(x) = x^3$. 🔢]
#only("1")[
#voiceover("First, we substitute the given value of x into the function.")
]
#only("2")[
#voiceover("Next, we calculate x raised to the power of n using the given exponent.")
]
#only("3")[
#voiceover("For example, let's evaluate f of 2 for the function f of x equals x cubed.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example Calculation 🔍]
#v(40pt)
#only("1-")[$f(x) = x^3$]
#only("2-")[$f(2) = 2^3$]
#only("3-")[$= 8$]
#only("1")[
#voiceover("Given the function f of x equals x cubed, we want to find f of 2.")
]
#only("2")[
#voiceover("We substitute 2 for x, so we get 2 cubed.")
]
#only("3")[
#voiceover("Calculating 2 cubed, we get 8.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary 📝]
#v(40pt)
#only("1-")[- To evaluate power functions, substitute $x$ and calculate $x^n$. 🔄]
#only("2-")[- Example: $f(x) = x^3$, $f(2) = 2^3 = 8$. 📊]
#only("3-")[- Practice with different values of $x$ and $n$. 🧩]
#only("1")[
#voiceover("To summarize, to evaluate power functions, you substitute the given value of x and calculate x raised to the power of n.")
]
#only("2")[
#voiceover("For example, with the function f of x equals x cubed, substituting 2 for x gives us 2 cubed, which equals 8.")
]
#only("3")[
#voiceover("Practice with different values of x and n to become more comfortable with evaluating power functions.")
]
]