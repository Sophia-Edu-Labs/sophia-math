#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
  #text(size: 30pt, weight: "bold")[Adding Four Single-Digit Integers]
  #v(40pt)

  #only("1-")[🧮 Everyday math skill]
  #v(20pt)
  #only("2-")[💰 Example: Calculating total cost]
  #v(20pt)
  #only("3-")[🍕 Example: Sharing pizza slices]
  #v(20pt)
  #only("4-")[📊 Sum up all given numbers]

  #only("1")[
    #voiceover("Welcome to our lesson on adding four single-digit integers. This is a fundamental math skill that we use in everyday life.")
  ]
  #only("2")[
    #voiceover("For instance, imagine you're shopping and need to quickly calculate the total cost of four items priced at 5 dollars, 3 dollars, 7 dollars, and 2 dollars.")
  ]
  #only("3")[
    #voiceover("Or perhaps you're sharing a pizza with friends and want to know how many slices you have in total if everyone took 2, 3, 1, and 4 slices respectively.")
  ]
  #only("4")[
    #voiceover("In both cases, we're adding four single-digit integers. The process involves summing up all the given numbers to get a total.")
  ]
]


#slide()[
  #text(size: 30pt, weight: "bold")[The Process]
  #v(40pt)

  #only("1-")[1️⃣ Identify the four numbers]
  #v(20pt)
  #only("2-")[2️⃣ Add from left to right]
  #v(20pt)
  #only("3-")[3️⃣ Keep a running total]
  #v(20pt)
  #only("4-")[4️⃣ Final sum = total of all numbers]

  #only("1")[
    #voiceover("Let's break down the process of adding four single-digit integers. First, we need to identify the four numbers we're working with.")
  ]
  #only("2")[
    #voiceover("Next, we'll add these numbers from left to right. This means we start with the first number and then add each subsequent number to our running total.")
  ]
  #only("3")[
    #voiceover("As we go, we keep a running total in our head or on paper. This running total changes each time we add a new number.")
  ]
  #only("4")[
    #voiceover("Finally, after we've added all four numbers, our running total becomes our final sum. This sum represents the total of all four numbers added together.")
  ]
]


#slide()[
  #text(size: 30pt, weight: "bold")[Recap]
  #v(40pt)

  #only("1-")[🔢 Adding four single-digit integers]
  #v(20pt)
  #only("2-")[➕ Sum all numbers for total]
  #v(20pt)
  #only("3-")[🛒 Useful in everyday situations]
  #v(20pt)
  #only("4-")[💡 Practice makes perfect!]

  #only("1")[
    #voiceover("To recap, adding four single-digit integers is a fundamental math skill that involves summing up all the given numbers to get a total.")
  ]
  #only("2")[
    #voiceover("The process is straightforward: we identify our four numbers, add them from left to right, keeping a running total, until we reach our final sum.")
  ]
  #only("3")[
    #voiceover("This skill is incredibly useful in many everyday situations, from shopping to sharing food with friends.")
  ]
  #only("4")[
    #voiceover("Remember, practice makes perfect! The more you work with adding single-digit integers, the quicker and more confident you'll become. In our next lesson, we'll look at some specific examples and techniques to make this process even easier.")
  ]
]