#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#only("1")[
#voiceover("That's okay, let's go through the correct solution step by step together.")
]

#text(size: 30pt, weight: "bold")[Multiplying Fractions]

#v(40pt)

#only("2-")[#text()[$1/2 dot 3/5$]]
// The multiplication of fractions is shown from slide 2 onward

#only("2")[
#voiceover("We start with the expression one-half multiplied by three-fifths.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Solution]

#v(40pt)

#only("1-")[- Multiply the numerators]
// The step of multiplying the numerators is shown from slide 1 onward

#only("2-")[- Multiply the denominators]
// The step of multiplying the denominators is shown from slide 2 onward

#only("3-")[- Simplify the fraction]
// The step of simplifying the fraction is shown from slide 3 onward

#only("1")[
#voiceover("To multiply two fractions, we follow these steps: First, we multiply the numerators.")
]

#only("2")[
#voiceover("Next, we multiply the denominators.")
]

#only("3")[
#voiceover("Finally, we simplify the resulting fraction if possible.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Multiplying the Numerators]

#v(40pt)

#only("1-")[$1 dot 3$]
// The multiplication of the numerators is shown from slide 1 onward

#v(10pt)

#only("2-")[$= 3$]
// The result of the multiplication is shown from slide 2 onward

#only("1")[
#voiceover("First, we multiply the numerators: one times three.")
]

#only("2")[
#voiceover("This gives us three.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Multiplying the Denominators]

#v(40pt)

#only("1-")[$2 dot 5$]
// The multiplication of the denominators is shown from slide 1 onward

#v(10pt)

#only("2-")[$= 10$]
// The result of the multiplication is shown from slide 2 onward

#only("1")[
#voiceover("Next, we multiply the denominators: two times five.")
]

#only("2")[
#voiceover("This gives us ten.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Simplifying the Fraction]

#v(40pt)

#only("1-")[$3/10$]
// The resulting fraction is shown from slide 1 onward

#only("1")[
#voiceover("So, the product of one-half and three-fifths is three-tenths.")
]
]