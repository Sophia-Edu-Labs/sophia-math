#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#only("1")[
#voiceover("That's okay! Let's go through the correct solution step by step together.")
]

#text(size: 30pt, weight: "bold")[Converting Complex Fractions to Decimals]

#v(40pt)

#only("2-")[#text()[$frac(9, 20)$]]
#only("2")[
#voiceover("We start with the fraction nine over twenty.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Conversion]

#v(40pt)

#only("1-")[- Step 1: Divide the numerator by the denominator. ➗]
#v(20pt)
#only("2-")[- Step 2: Simplify the result. 🧮]

#only("1")[
#voiceover("To convert the fraction to a decimal, we follow these steps. First, we divide the numerator by the denominator.")
]
#only("2")[
#voiceover("Next, we simplify the result to get the decimal form.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $frac(9, 20)$ to a Decimal]

#v(40pt)

#only("1-")[- Step 1: $9 ÷ 20 = 0.45$ ➗]
#v(20pt)
#only("2-")[- Step 2: Simplified result is $0.45$ 🧮]

#only("1")[
#voiceover("Let's apply these steps to the fraction nine over twenty. First, we divide nine by twenty, which equals zero point four five.")
]
#only("2")[
#voiceover("Since zero point four five is already in its simplest form, we don't need to simplify further. So, the decimal representation of nine over twenty is zero point four five.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary]

#v(40pt)

#only("1-")[- We converted $frac(9, 20)$ to $0.45$. 📜]

#only("1")[
#voiceover("To summarize, we converted the fraction nine over twenty to its decimal form, which is zero point four five.")
]
]