#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#only("1")[
#voiceover("Great job on recognizing the correct answer! Let's go through the solution step by step together.")
]

#text(size: 30pt, weight: "bold")[Converting Decimals to Fractions]

#v(40pt)

#only("2-")[#text()[$7.910$]]
// The decimal number 7.910 is shown from slide 2 onward

#only("2")[
#voiceover("We are given the decimal number seven point nine one zero.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Steps to Convert Decimals to Fractions]

#v(40pt)

#only("1-")[- Step 1: Count the decimal places. 🔢]
#v(20pt)
#only("2-")[- Step 2: Remove the decimal point. ✂️]
#v(20pt)
#only("3-")[- Step 3: Divide by a power of ten. ➗]
#v(20pt)
#only("4-")[- Step 4: Simplify the fraction if possible. 🔄]

#only("1")[
#voiceover("Before we solve the exercise, let's quickly review the steps to convert decimals to fractions. First, count the number of decimal places in the decimal number.")
]
#only("2")[
#voiceover("Next, remove the decimal point from the number.")
]
#only("3")[
#voiceover("Then, divide the resulting number by a one followed by zeroes, where the number of zeroes equals the number of decimal places.")
]
#only("4")[
#voiceover("Finally, simplify the fraction if possible.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $7.910$ to a Fraction]

#v(40pt)

#only("1-")[- Step 1: 🔢 Count the decimal places in $7.910$. $arrow$ There are 3 decimal places.]
#v(20pt)
#only("2-")[- Step 2: ✂️ Remove the decimal point: $arrow 7910$.]
#v(20pt)
#only("3-")[- Step 3: ➗ Divide by $1000 arrow 7910/1000$.]
#v(20pt)
#only("4-")[- Step 4: 🔄 Simplify the fraction: $arrow 791/100$.]

#only("1")[
#voiceover("Now let's apply these steps to the decimal number seven point nine one zero. First, we count the decimal places. There are three decimal places.")
]
#only("2")[
#voiceover("Next, we remove the decimal point, which gives us seven thousand nine hundred ten.")
]
#only("3")[
#voiceover("Then, we divide seven thousand nine hundred ten by a one followed by three zeroes. Since seven point nine one zero has three decimal places, we divide it by one thousand. So we get seven thousand nine hundred ten over one thousand.")
]
#only("4")[
#voiceover("Finally, we simplify the fraction if possible. In this case, seven thousand nine hundred ten over one thousand simplifies to seven hundred ninety-one over one hundred.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Final Answer]

#v(40pt)

#only("1-")[The fraction equivalent of $7.910$ is $791/100$]

#only("1")[
#voiceover("So, the fraction equivalent of seven point nine one zero is seven hundred ninety-one over one hundred. Well done!")
]
]