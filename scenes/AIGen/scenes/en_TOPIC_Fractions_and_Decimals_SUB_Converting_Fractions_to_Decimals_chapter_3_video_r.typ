#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Review: Converting Fractions to Decimals]
#v(40pt)
#only("1-")[- Converting fractions to decimals is essential in many real-life scenarios. 🛒]
#only("2-")[- Let's review how to convert a simple fraction to a decimal. 🔄]
#only("1")[
#voiceover("Let's review the process of converting fractions to decimals. This is essential in many real-life scenarios, such as shopping or measuring ingredients.")
]
#only("2")[
#voiceover("We'll go through the steps to convert a simple fraction to a decimal.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Conversion]
#v(40pt)
#only("1-")[- Step 1: Find an equivalent fraction with a power of ten as the denominator. 🔍]
#v(20pt)
#only("2-")[- Step 2: Divide the numerator by the denominator. ➗]
#v(20pt)
#only("3-")[- Example: $3/4$ to $75/100$ to $0.75$]
#only("1")[
#voiceover("First, find an equivalent fraction with a power of ten as the denominator.")
]
#only("2")[
#voiceover("Next, divide the numerator by the denominator.")
]
#only("3")[
#voiceover("For example, to convert three-fourths to a decimal, we find an equivalent fraction with a denominator of one hundred, which is seventy-five over one hundred. Dividing seventy-five by one hundred gives us zero point seventy-five.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $3/4$ to a Decimal]
#v(40pt)
#only("1-")[- Step 1: Multiply numerator and denominator by $25$ to get $75/100$. ➕]
#v(20pt)
#only("2-")[- Step 2: Divide $75$ by $100$ to get $0.75$. ➗]
#v(20pt)
#only("1")[
#voiceover("Let's see an example. To convert three-fourths to a decimal, multiply the numerator and denominator by twenty-five to get seventy-five over one hundred.")
]
#only("2")[
#voiceover("Then, divide seventy-five by one hundred to get zero point seventy-five.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary]
#v(40pt)
#only("1-")[- Find an equivalent fraction with a power of ten as the denominator. 🔍]
#v(20pt)
#only("2-")[- Divide the numerator by the denominator. ➗]
#v(20pt)
#only("3-")[- Example: $3/4$ to $75/100$ to $0.75$]
#only("1")[
#voiceover("To summarize, first find an equivalent fraction with a power of ten as the denominator.")
]
#only("2")[
#voiceover("Then, divide the numerator by the denominator.")
]
#only("3")[
#voiceover("For example, three-fourths becomes seventy-five over one hundred, which equals zero point seventy-five.")
]
]