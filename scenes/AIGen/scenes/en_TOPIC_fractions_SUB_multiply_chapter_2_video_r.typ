#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Review: Multiplying Fractions 🧮]
#v(40pt)
#only("1-")[- Multiplying fractions is useful in many real-life situations. 🍰]
#only("2-")[- For example, when scaling recipes or calculating probabilities. 📊]
#only("3-")[- Let's review how to multiply multiple fractions. 🔄]
#only("1")[
#voiceover("Let's review the concept of multiplying fractions. Multiplying fractions is useful in many real-life situations.")
]
#only("2")[
#voiceover("For example, when scaling recipes or calculating probabilities.")
]
#only("3")[
#voiceover("Let's review how to multiply multiple fractions.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Review]
#v(40pt)
#only("1-")[- Multiply the numerators together. 🔢]
#only("2-")[- Multiply the denominators together. ➗]
#only("3-")[- Simplify the resulting fraction if possible. ✂️]
#only("1")[
#voiceover("First, multiply all the numerators together.")
]
#only("2")[
#voiceover("Next, multiply all the denominators together.")
]
#only("3")[
#voiceover("Finally, simplify the resulting fraction if possible.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Multiplying Fractions]
#v(40pt)
#only("1-")[- Consider the fractions $a/b$, $c/d$, and $e/f$.]
#only("2-")[- Multiply the numerators: $a dot c dot e$.]
#only("3-")[- Multiply the denominators: $b dot d dot f$.]
#only("4-")[- Resulting fraction: $(a dot c dot e)/(b dot d dot f)$.]
#only("5-")[- Simplify if possible. 🧮]
#only("1")[
#voiceover("Consider the fractions a over b, c over d, and e over f.")
]
#only("2")[
#voiceover("First, multiply the numerators: a times c times e.")
]
#only("3")[
#voiceover("Next, multiply the denominators: b times d times f.")
]
#only("4")[
#voiceover("The resulting fraction is a times c times e over b times d times f.")
]
#only("5")[
#voiceover("Finally, simplify the fraction if possible.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary 📝]
#v(40pt)
#only("1-")[- Multiply numerators together. 🔢]
#only("2-")[- Multiply denominators together. ➗]
#only("3-")[- Simplify the resulting fraction. ✂️]
#only("1")[
#voiceover("To recap, to multiply multiple fractions, multiply all the numerators together.")
]
#only("2")[
#voiceover("Then, multiply all the denominators together.")
]
#only("3")[
#voiceover("Finally, simplify the resulting fraction if possible.")
]
]