#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#only("1")[
#voiceover("Great job on attempting the exercise! Let's go through the solution step by step together.")
]

#text(size: 30pt, weight: "bold")[Multiplying Fractions]

#v(40pt)

#only("2-")[#text()[$3 dot 1/2$]]
#only("2")[
#voiceover("We start with the expression 3 times one-half.")
]

]


#slide()[
#only("1")[
#voiceover("To multiply a whole number by a fraction, we multiply the whole number by the numerator of the fraction and keep the denominator the same.")
]

#text(size: 30pt, weight: "bold")[Step-by-Step Solution]

#v(40pt)

#only("1-")[- Multiply the numerator by the whole number]
#v(10pt)
#only("2-")[- Keep the denominator the same]

#only("2")[
#voiceover("First, we multiply the numerator by the whole number.")
]

#only("3")[
#voiceover("Next, we keep the denominator the same.")
]

]


#slide()[
#text(size: 30pt, weight: "bold")[Solution]

#v(40pt)

#only("1-")[$3 dot 1/2 = 3/2$]
#v(10pt)
#only("2-")[#text(fill:aqua)[⇓ Simplified Result]]
#v(10pt)
#only("3-")[$3/2$]

#only("1")[
#voiceover("So, multiplying 3 by one-half, we get 3 times 1 divided by 2.")
]

#only("2")[
#voiceover("This simplifies to three-halves.")
]

]


#slide()[
#text(size: 30pt, weight: "bold")[Final Answer]

#v(40pt)

#only("1-")[$3 dot 1/2 = 3/2$]
#v(10pt)
#only("2-")[#text(fill:aqua)[Well done! 🎉]]

#only("1")[
#voiceover("Therefore, the final answer is three-halves.")
]

#only("2")[
#voiceover("Well done! Keep practicing and you'll master multiplying fractions in no time. 🎉")
]

]