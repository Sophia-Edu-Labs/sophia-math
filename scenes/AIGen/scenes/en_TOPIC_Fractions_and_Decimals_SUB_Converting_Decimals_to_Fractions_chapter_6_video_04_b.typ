#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#only("1")[
#voiceover("No worries, let's go through the correct solution step by step together.")
]

#text(size: 30pt, weight: "bold")[Converting $0.99$ to a Fraction]

#v(40pt)

#only("2-")[#text()[$0.99 = 99/100$]]
// The fraction representation of 0.99 is shown from slide 2 onward

#only("2")[
#voiceover("To convert zero point nine nine to a fraction, we first recognize that zero point nine nine is equivalent to ninety-nine divided by one hundred.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Steps to Convert Decimals to Fractions]
#v(40pt)
#only("1-")[- Step 1: Count the decimal places. 🔢]
#v(20pt)
#only("2-")[- Step 2: Remove the decimal point. ✂️]
#v(20pt)
#only("3-")[- Step 3: Divide by a power of ten. ➗]
#only("1")[
#voiceover("Before we solve the exercise, let's quickly review the steps to convert decimals to fractions. First, count the number of decimal places in the decimal number.")
]
#only("2")[
#voiceover("Next, remove the decimal point from the number.")
]
#only("3")[
#voiceover("Then, divide the resulting number by a one followed by zeroes, where the number of zeroes equals the number of decimal places.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $0.99$ to a Fraction]
#v(40pt)
#only("1-")[- Step 1: 🔢 Count the decimal places in $0.99$. $arrow$There are 2 decimal places.]
#v(20pt)
#only("2-")[- Step 2: ✂️ Remove the decimal point: $arrow 99$. ]
#v(20pt)
#only("3-")[- Step 3: ➗ Divide by $100 arrow 99/100$ ]

#only("1")[
#voiceover("Now let's apply these steps to the decimal number zero point nine nine. To convert zero point nine nine to a fraction, we first count the decimal places. There are two decimal places.")
]
#only("2")[
#voiceover("Next, we remove the decimal point, which gives us ninety-nine.")
]
#only("3")[
#voiceover("Then, we divide ninety-nine by a one followed by two zeroes. Since zero point nine nine has two decimal places, we divide it by one hundred. So we get ninety-nine over one hundred. And that's it, we're done! The solution is ninety-nine over one hundred.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Simplifying the Fraction]
#v(40pt)
#only("1-")[- Step 4: Simplify the fraction if possible. ➗]
#v(20pt)
#only("2-")[- $99/100$ is already in its simplest form. ✔️]

#only("1")[
#voiceover("Finally, we check if the fraction can be simplified. In this case, ninety-nine over one hundred is already in its simplest form.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Conclusion]
#v(40pt)
#only("1-")[The fraction equivalent of $0.99$ is $99/100$. 🎉]

#only("1")[
#voiceover("So, the fraction equivalent of zero point nine nine is ninety-nine over one hundred. Great job!")
]
]