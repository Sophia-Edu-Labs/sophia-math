#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#only("1")[
#voiceover("Great job! Let's go through the solution step-by-step together.")
]

#text(size: 30pt, weight: "bold")[Converting Decimals to Fractions]

#v(40pt)

#only("2-")[#text()[$0.9$]]
#only("2")[
#voiceover("We are given the decimal zero point nine.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Steps to Convert Decimals to Fractions]
#v(40pt)
#only("1-")[- Step 1: Count the decimal places. 🔢]
#v(20pt)
#only("2-")[- Step 2: Remove the decimal point. ✂️]
#v(20pt)
#only("3-")[- Step 3: Divide by a power of ten. ➗]

#only("1")[
#voiceover("Before we solve the exercise, let's quickly review the steps to convert decimals to fractions. First, count the number of decimal places in the decimal number.")
]
#only("2")[
#voiceover("Next, remove the decimal point from the number.")
]
#only("3")[
#voiceover("Then, divide the resulting number by a one followed by zeroes, where the number of zeroes equals the number of decimal places.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $0.9$ to a Fraction]
#v(40pt)
#only("1-")[- Step 1: 🔢 Count the decimal places in $0.9$. $arrow$There is 1 decimal place.]
#v(20pt)
#only("2-")[- Step 2: ✂️ Remove the decimal point: $arrow 9$. ]
#v(20pt)
#only("3-")[- Step 3: ➗ Divide by $10 arrow 9/10$ ]

#only("1")[
#voiceover("Now let's apply these steps to the decimal number zero point nine. First, we count the decimal places. There is one decimal place.")
]
#only("2")[
#voiceover("Next, we remove the decimal point, which gives us nine.")
]
#only("3")[
#voiceover("Then, we divide nine by a one followed by one zero. Since zero point nine has one decimal place, we divide it by ten. So we get nine over ten. And that's it, we're done! The solution is nine over ten.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary]
#v(40pt)
#only("1-")[- Decimal: $0.9$]
#v(20pt)
#only("2-")[- Fraction: $9/10$]

#only("1")[
#voiceover("To summarize, we converted the decimal zero point nine to the fraction nine over ten.")
]
]