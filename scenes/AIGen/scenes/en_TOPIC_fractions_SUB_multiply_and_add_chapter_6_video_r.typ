#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
  #text(size: 30pt, weight: "bold")[Review: Adding Fractions 🧮]
  #v(40pt)
  #only("1-")[- Fractions represent parts of a whole 🍕]
  #only("2-")[- Adding fractions is a common operation 🔢]
  #only("3-")[- Let's recap the process 📝]

  #only("1")[
    #voiceover("Let's review the process of adding fractions. Fractions are a way to represent parts of a whole.")
  ]
  #only("2")[
    #voiceover("Adding fractions is a common operation we encounter in many real-life situations, like cooking or measuring.")
  ]
  #only("3")[
    #voiceover("Let's recap the key steps involved in adding fractions.")
  ]
]


#slide()[
  #text(size: 30pt, weight: "bold")[The Process 📊]
  #v(40pt)
  #only("1-")[1. Find a common denominator 🔍]
  #v(20pt)
  #only("2-")[2. Convert fractions to equivalent fractions 🔄]
  #v(20pt)
  #only("3-")[3. Add the numerators ➕]
  #v(20pt)
  #only("4-")[4. Simplify if possible ✂️]

  #only("1")[
    #voiceover("The first step in adding fractions is to find a common denominator. This is crucial because we can only add fractions when they have the same denominator.")
  ]
  #only("2")[
    #voiceover("Next, we convert each fraction to an equivalent fraction with this common denominator. Remember, equivalent fractions represent the same value.")
  ]
  #only("3")[
    #voiceover("Once we have fractions with the same denominator, we can add the numerators while keeping the denominator the same.")
  ]
  #only("4")[
    #voiceover("Finally, if possible, we simplify the resulting fraction by dividing both the numerator and denominator by their greatest common factor.")
  ]
]


#slide()[
  #text(size: 30pt, weight: "bold")[Example 🌟]
  #v(40pt)
  #only("1-")[Adding $1/3$ and $1/4$]
  #v(20pt)
  #only("2-")[Common denominator: $12$]
  #v(20pt)
  #only("3-")[$1/3 = 4/12$ and $1/4 = 3/12$]
  #v(20pt)
  #only("4-")[$4/12 + 3/12 = 7/12$]

  #only("1")[
    #voiceover("Let's walk through an example. Suppose we want to add one-third and one-fourth.")
  ]
  #only("2")[
    #voiceover("First, we need to find a common denominator. The least common multiple of 3 and 4 is 12, so we'll use 12 as our common denominator.")
  ]
  #only("3")[
    #voiceover("Now, we convert each fraction to an equivalent fraction with the denominator 12. One-third becomes four-twelfths, and one-fourth becomes three-twelfths.")
  ]
  #only("4")[
    #voiceover("Finally, we add the numerators: four-twelfths plus three-twelfths equals seven-twelfths. This fraction can't be simplified further, so it's our final answer.")
  ]
]


#slide()[
  #text(size: 30pt, weight: "bold")[Summary 📝]
  #v(40pt)
  #only("1-")[- Common denominator is key 🔑]
  #only("2-")[- Convert to equivalent fractions ↔️]
  #only("3-")[- Add numerators, keep denominator ➕]
  #only("4-")[- Simplify if possible 🔍]

  #only("1")[
    #voiceover("To summarize, remember that finding a common denominator is the key to adding fractions.")
  ]
  #only("2")[
    #voiceover("Always convert your fractions to equivalent fractions with this common denominator.")
  ]
  #only("3")[
    #voiceover("Then, add the numerators while keeping the denominator the same.")
  ]
  #only("4")[
    #voiceover("Finally, simplify your result if possible. With these steps, you'll be able to add fractions confidently!")
  ]
]