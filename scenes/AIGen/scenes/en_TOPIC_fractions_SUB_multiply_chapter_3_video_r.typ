#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Review: Multiplying Fractions with Whole Numbers, Mixed Numbers, and Improper Fractions]
#v(40pt)
#only("1-")[- Multiplying fractions is essential in many real-world situations. 🍰]
#only("2-")[- Let's review the process step by step. 📚]
#only("1")[
#voiceover("Let's review how to multiply fractions, especially when dealing with whole numbers, mixed numbers, and improper fractions. This is essential in many real-world situations, like dividing a cake into equal parts.")
]
#only("2")[
#voiceover("We'll go through the process step by step.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Review]
#v(40pt)
#only("1-")[- Step 1: Convert all numbers to improper fractions. 🔄]
#only("2-")[- Step 2: Multiply the numerators. ✖️]
#only("3-")[- Step 3: Multiply the denominators. ➗]
#only("4-")[- Step 4: Simplify the fraction if possible. ✂️]
#only("1")[
#voiceover("First, convert all numbers to improper fractions. For example, convert a whole number like 3 to 3 over 1, and a mixed number like 2 and 1/2 to 5 over 2.")
]
#only("2")[
#voiceover("Next, multiply the numerators. For example, if we have 3 over 1 and 5 over 2, we multiply 3 by 5.")
]
#only("3")[
#voiceover("Then, multiply the denominators. In our example, we multiply 1 by 2.")
]
#only("4")[
#voiceover("Finally, simplify the fraction if possible. In our example, 15 over 2 is already in its simplest form.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example]
#v(40pt)
#only("1-")[- $3$ $times$ $2 1/2$]
#only("2-")[- Convert: $3$ $arrow$ $3/1$, $2 1/2$ $arrow$ $5/2$]
#only("3-")[- Multiply: $3/1$ $times$ $5/2$ $arrow$ $15/2$]
#only("4-")[- Simplify: $15/2$ $arrow$ $7 1/2$]
#only("1")[
#voiceover("Let's look at an example. Multiply 3 by 2 and a half.")
]
#only("2")[
#voiceover("First, convert 3 to 3 over 1, and 2 and a half to 5 over 2.")
]
#only("3")[
#voiceover("Next, multiply the numerators, 3 times 5, and the denominators, 1 times 2, to get 15 over 2.")
]
#only("4")[
#voiceover("Finally, simplify 15 over 2 to 7 and a half.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary 📝]
#v(40pt)
#only("1-")[- Convert all numbers to improper fractions. 🔄]
#only("2-")[- Multiply the numerators. ✖️]
#only("3-")[- Multiply the denominators. ➗]
#only("4-")[- Simplify the fraction if possible. ✂️]
#only("1")[
#voiceover("To recap, when multiplying fractions with whole numbers, mixed numbers, or improper fractions, first convert all numbers to improper fractions.")
]
#only("2")[
#voiceover("Then, multiply the numerators.")
]
#only("3")[
#voiceover("Next, multiply the denominators.")
]
#only("4")[
#voiceover("Finally, simplify the fraction if possible.")
]
]