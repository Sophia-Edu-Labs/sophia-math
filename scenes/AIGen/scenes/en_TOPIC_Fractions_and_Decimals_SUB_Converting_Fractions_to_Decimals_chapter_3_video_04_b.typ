#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Converting Fractions to Decimals]
#v(40pt)
#only("1")[
#voiceover("That's okay! Let's go through the correct solution step-by-step together.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Conversion]
#v(40pt)
#only("1-")[- Step 1: Divide the numerator by the denominator. ➗]
#v(20pt)
#only("2-")[- Step 2: Write down the result. ✍️]

#only("1")[
#voiceover("To convert a fraction to a decimal, we follow two simple steps. First, we divide the numerator by the denominator.")
]
#only("2")[
#voiceover("Second, we write down the result.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $1/5$ to a Decimal]
#v(40pt)
#only("1-")[- Step 1: 🔢 Divide $1$ by $5$.]
#v(20pt)
#only("2-")[- Step 2: ✍️ Write down the result: $0.2$.]

#only("1")[
#voiceover("Now let's apply these steps to the fraction one-fifth. First, we divide one by five.")
]
#only("2")[
#voiceover("The result is zero point two. So, one-fifth as a decimal is zero point two.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Recap]
#v(40pt)
#only("1-")[- To convert $1/5$ to a decimal: ➗ Divide $1$ by $5$ $arrow 0.2$.]

#only("1")[
#voiceover("To recap, to convert one-fifth to a decimal, we divide one by five, which gives us zero point two. And that's it! You've successfully converted the fraction to a decimal.")
]
]