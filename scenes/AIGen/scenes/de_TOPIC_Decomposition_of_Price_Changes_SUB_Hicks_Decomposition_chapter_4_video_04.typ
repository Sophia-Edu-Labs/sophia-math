#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]

#slide()[
#text(size: 30pt, weight: "bold")[Hicks-Zerlegung Rückblick 🎓]
#v(40pt)
#only("1-")[- Trennt die Preiseffekte in Substitutions- und Einkommenseffekte]
#only("1")[
#voiceover("In diesem Rückblick werden wir die wichtigsten Punkte der Hicks-Zerlegung wiederholen. Die Hicks-Zerlegung trennt die Effekte einer Preisänderung in Substitutions- und Einkommenseffekte.")
]
#v(20pt)
#only("2-")[- Hält den Nutzen konstant 📈]
#only("2")[
#voiceover("Ein wichtiger Aspekt der Hicks-Zerlegung ist, dass der Nutzen bei der Analyse dieser Effekte konstant gehalten wird.")
]
]

#slide()[
#text(size: 30pt, weight: "bold")[Substitutionseffekt 🔄]
#v(40pt)
#only("1-")[- Zeigt, wie sich der Konsum ändert, wenn sich der Preis ändert, während der Nutzen konstant bleibt]
#only("1")[
#voiceover("Der Substitutionseffekt in der Hicks-Zerlegung zeigt, wie sich der Konsum ändert, wenn sich der Preis ändert, während der Nutzen konstant bleibt.")
]
#v(20pt)
#only("2-")[- Wenn der Preis sinkt, steigt die nachgefragte Menge]
#only("2")[
#voiceover("Wenn der Preis eines Gutes sinkt, wird die nachgefragte Menge dieses Gutes aufgrund des Substitutionseffekts steigen.")
]
]

#slide()[
#text(size: 30pt, weight: "bold")[Einkommenseffekt 💰]
#v(40pt)
#only("1-")[- Zeigt, wie sich der Konsum aufgrund einer Änderung des realen Einkommens ändert, nach Anpassung des Substitutionseffekts]
#only("1")[
#voiceover("Der Einkommenseffekt in der Hicks-Zerlegung zeigt, wie sich der Konsum aufgrund einer Änderung des realen Einkommens ändert, nachdem der Substitutionseffekt angepasst wurde.")
]
#v(20pt)
#only("2-")[- Wenn der Preis sinkt, steigt das reale Einkommen]
#only("2")[
#voiceover("Wenn der Preis eines Gutes sinkt, wird das reale Einkommen des Verbrauchers steigen.")
]
]

#slide()[
#text(size: 30pt, weight: "bold")[Mathematische Darstellung 📜]
#v(40pt)
#only("1-")[- Verwendet kompensierte Nachfragefunktionen]
#only("1")[
#voiceover("Die Hicks-Zerlegung verwendet kompensierte Nachfragefunktionen, um die Substitutions- und Einkommenseffekte einer Preisänderung mathematisch darzustellen.")
]
#v(20pt)
#only("2-")[- Kompensierte Nachfragefunktion: $h_X(p_X, p_Y, U)$]
#only("2")[
#voiceover("Die kompensierte Nachfragefunktion für ein Gut X wird als h Index X von p Index X, p Index Y und U geschrieben, wobei p Index X und p Index Y die Preise der Güter X und Y und U das Nutzenniveau sind.")
]
]

#slide()[
#text(size: 30pt, weight: "bold")[Wichtige Erkenntnisse 🔑]
#v(40pt)
#only("1-")[- Die Hicks-Zerlegung trennt die Preiseffekte in Substitutions- und Einkommenseffekte]
#only("1")[
#voiceover("Zusammenfassend trennt die Hicks-Zerlegung die Effekte einer Preisänderung in Substitutions- und Einkommenseffekte.")
]
#v(20pt)
#only("2-")[- Hält den Nutzen konstant]
#only("2")[
#voiceover("Sie hält den Nutzen bei der Analyse dieser Effekte konstant.")
]
#v(20pt)
#only("3-")[- Verwendet kompensierte Nachfragefunktionen zur mathematischen Darstellung]
#only("3")[
#voiceover("Und sie verwendet kompensierte Nachfragefunktionen, um diese Effekte mathematisch darzustellen.")
]
]