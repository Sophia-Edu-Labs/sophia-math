#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Multiplying Fractions]
#v(40pt)
#only("1-")[- Situation: Sharing a cake 🍰]
#v(20pt)
#only("2-")[- Need: Multiply fractions to find out how much each person gets]
#v(20pt)
#only("3-")[- Example: $1/2 * 1/3 * 1/4$]
#v(20pt)
#only("4-")[- Let's learn how to multiply fractions!]
#only("1")[
#voiceover("Imagine you have a cake and you want to share it with friends. You need to multiply fractions to find out how much each person gets.")]
#only("2")[
#voiceover("To do this, we need to multiply fractions. For example, let's consider the fractions one half, one third, and one fourth.")]
#only("3")[
#voiceover("We will multiply these fractions to find out how much each person gets.")]
#only("4")[
#voiceover("Let's learn how to multiply fractions!")]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Explanation]
#v(40pt)
#only("1-")[- Step 1: Multiply the numerators]
#v(20pt)
#only("2-")[- Example: $1 * 1 * 1 = 1$]
#v(20pt)
#only("3-")[- Step 2: Multiply the denominators]
#v(20pt)
#only("4-")[- Example: $2 * 3 * 4 = 24$]
#v(20pt)
#only("5-")[- Result: $1/24$]
#only("1")[
#voiceover("First, we multiply the numerators of the fractions.")]
#only("2")[
#voiceover("In our example, we multiply one times one times one, which equals one.")]
#only("3")[
#voiceover("Next, we multiply the denominators of the fractions.")]
#only("4")[
#voiceover("In our example, we multiply two times three times four, which equals twenty-four.")]
#only("5")[
#voiceover("So, the result of multiplying one half, one third, and one fourth is one twenty-fourth.")]
]


#slide()[
#text(size: 30pt, weight: "bold")[Recap]
#v(40pt)
#only("1-")[- Multiply the numerators together]
#v(20pt)
#only("2-")[- Multiply the denominators together]
#v(20pt)
#only("3-")[- Example: $1/2 * 1/3 * 1/4 = 1/24$]
#only("1")[
#voiceover("To recap, when multiplying fractions, first multiply the numerators together.")]
#only("2")[
#voiceover("Then, multiply the denominators together.")]
#only("3")[
#voiceover("For example, multiplying one half, one third, and one fourth gives us one twenty-fourth.")]
]