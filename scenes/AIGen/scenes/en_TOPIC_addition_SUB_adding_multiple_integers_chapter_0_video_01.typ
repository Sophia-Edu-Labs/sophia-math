#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
  #text(size: 30pt, weight: "bold")[Adding Three Single-Digit Integers]
  #v(40pt)

  #only("1-")[🧮 Simple yet fundamental operation]
  #v(20pt)
  #only("2-")[💡 Used in everyday situations]
  #v(20pt)
  #only("3-")[🔢 Involves summing up all given numbers]
  #v(20pt)
  #only("4-")[➕ Results in a total]

  #only("1")[
    #voiceover("Welcome to our lesson on adding three single-digit integers. This operation might seem simple, but it's a fundamental skill in mathematics.")
  ]

  #only("2")[
    #voiceover("We use this skill in many everyday situations. For example, when we're counting the total number of items in our shopping cart, or adding up the scores in a game.")
  ]

  #only("3")[
    #voiceover("Adding three single-digit integers involves summing up all the given numbers. Let's break it down step by step.")
  ]

  #only("4")[
    #voiceover("The result of this addition is a total, which represents the combined value of all three numbers.")
  ]
]


#slide()[
  #text(size: 30pt, weight: "bold")[Step-by-Step Process]
  #v(40pt)

  #only("1-")[1️⃣ Identify the three single-digit integers]
  #v(20pt)
  #only("2-")[2️⃣ Add the first two numbers]
  #v(20pt)
  #only("3-")[3️⃣ Add the result to the third number]
  #v(20pt)
  #only("4-")[4️⃣ The final sum is the total]

  #only("1")[
    #voiceover("Let's go through the process step by step. First, we need to identify our three single-digit integers. Remember, single-digit integers are whole numbers from 0 to 9.")
  ]

  #only("2")[
    #voiceover("Next, we start by adding the first two numbers. This gives us an intermediate sum.")
  ]

  #only("3")[
    #voiceover("Then, we take this intermediate sum and add it to the third number.")
  ]

  #only("4")[
    #voiceover("The result of this final addition is our total sum. This represents the combined value of all three original numbers.")
  ]
]


#slide()[
  #text(size: 30pt, weight: "bold")[Example]
  #v(40pt)

  #only("1-")[Let's add: 7 + 5 + 3]
  #v(20pt)
  #only("2-")[Step 1: 7 + 5 = 12]
  #v(20pt)
  #only("3-")[Step 2: 12 + 3 = 15]
  #v(20pt)
  #only("4-")[Final result: 7 + 5 + 3 = 15]

  #only("1")[
    #voiceover("Let's work through an example together. Suppose we want to add the numbers 7, 5, and 3.")
  ]

  #only("2")[
    #voiceover("We start by adding the first two numbers: 7 plus 5 equals 12.")
  ]

  #only("3")[
    #voiceover("Now we take this intermediate sum of 12 and add it to our third number, 3. So, 12 plus 3 equals 15.")
  ]

  #only("4")[
    #voiceover("Therefore, our final result is 15. We've successfully added three single-digit integers: 7 plus 5 plus 3 equals 15.")
  ]
]


#slide()[
  #text(size: 30pt, weight: "bold")[Summary]
  #v(40pt)

  #only("1-")[🔢 Adding three single-digit integers]
  #v(20pt)
  #only("2-")[🧮 Sum up all given numbers]
  #v(20pt)
  #only("3-")[➕ Result is the total of all three numbers]
  #v(20pt)
  #only("4-")[💡 Important skill for everyday math]

  #only("1")[
    #voiceover("To summarize, adding three single-digit integers is a fundamental mathematical operation.")
  ]

  #only("2")[
    #voiceover("It involves summing up all the given numbers, which we do step by step.")
  ]

  #only("3")[
    #voiceover("The result is a total that represents the combined value of all three original numbers.")
  ]

  #only("4")[
    #voiceover("This skill is important for many everyday situations and forms the basis for more complex mathematical operations. Keep practicing, and you'll master it in no time!")
  ]
]