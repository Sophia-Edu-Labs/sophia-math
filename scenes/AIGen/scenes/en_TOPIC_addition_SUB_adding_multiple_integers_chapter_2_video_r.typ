#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
  #text(size: 30pt, weight: "bold")[Review: Adding Four Single-Digit Integers 🧮]
  #v(40pt)
  #only("1-")[- Everyday math skill 🛒]
  #only("2-")[- Used in shopping, scoring games, etc. 🏀]
  #only("3-")[- Let's review the process step-by-step ✅]

  #only("1")[
    #voiceover("Let's review the process of adding four single-digit integers. This is a fundamental math skill we use in everyday life.")
  ]
  #only("2")[
    #voiceover("We often need this skill when shopping, scoring games, or in many other situations where we need to quickly sum up small numbers.")
  ]
  #only("3")[
    #voiceover("Let's go through the process step-by-step to refresh our memory.")
  ]
]


#slide()[
  #text(size: 30pt, weight: "bold")[The Process 📝]
  #v(40pt)
  #only("1-")[1️⃣ Identify the four single-digit integers]
  #v(10pt)
  #only("2-")[2️⃣ Add the first two numbers]
  #v(10pt)
  #only("3-")[3️⃣ Add the third number to the result]
  #v(10pt)
  #only("4-")[4️⃣ Finally, add the fourth number]
  #v(10pt)
  #only("5-")[5️⃣ The sum is your final answer]

  #only("1")[
    #voiceover("First, we need to identify our four single-digit integers. Remember, single-digit integers are whole numbers from 0 to 9.")
  ]
  #only("2")[
    #voiceover("Next, we start by adding the first two numbers together.")
  ]
  #only("3")[
    #voiceover("Then, we take the result of that addition and add the third number to it.")
  ]
  #only("4")[
    #voiceover("After that, we add the fourth and final number to our running total.")
  ]
  #only("5")[
    #voiceover("The result of this last addition is our final answer - the sum of all four single-digit integers.")
  ]
]


#slide()[
  #text(size: 30pt, weight: "bold")[Example 🌟]
  #v(40pt)
  #only("1-")[Let's add: 7 + 4 + 9 + 2]
  #v(20pt)
  #only("2-")[Step 1: 7 + 4 = 11]
  #v(10pt)
  #only("3-")[Step 2: 11 + 9 = 20]
  #v(10pt)
  #only("4-")[Step 3: 20 + 2 = 22]
  #v(20pt)
  #only("5-")[Final sum: 22]

  #only("1")[
    #voiceover("Let's walk through an example. Say we want to add the numbers 7, 4, 9, and 2.")
  ]
  #only("2")[
    #voiceover("We start by adding 7 and 4, which gives us 11.")
  ]
  #only("3")[
    #voiceover("Next, we add 9 to 11, resulting in 20.")
  ]
  #only("4")[
    #voiceover("Finally, we add the last number, 2, to 20.")
  ]
  #only("5")[
    #voiceover("This gives us our final sum of 22. And that's it! We've successfully added four single-digit integers.")
  ]
]


#slide()[
  #text(size: 30pt, weight: "bold")[Summary 📚]
  #v(40pt)
  #only("1-")[- Adding four single-digit integers is straightforward 🧮]
  #only("2-")[- Process: Add numbers one by one ➕]
  #only("3-")[- Practice makes perfect! 🏆]
  #only("4-")[- Useful skill in daily life 🛍️]

  #only("1")[
    #voiceover("To recap, adding four single-digit integers is a straightforward process.")
  ]
  #only("2")[
    #voiceover("We simply add the numbers one by one, keeping a running total as we go.")
  ]
  #only("3")[
    #voiceover("Remember, practice makes perfect. The more you do this, the faster and more accurate you'll become.")
  ]
  #only("4")[
    #voiceover("This is a valuable skill that you'll use often in daily life, from shopping to playing games and beyond.")
  ]
]