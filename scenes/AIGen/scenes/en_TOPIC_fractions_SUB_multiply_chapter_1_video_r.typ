#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Review: Multiplying Fractions 🧮]
#v(40pt)
#only("1-")[- Multiplying fractions is a common operation in math. ➗]
#only("2-")[- It's useful in various real-life situations, like cooking. 🍲]
#only("3-")[- Let's review the steps to multiply fractions. 📚]
#only("1")[
#voiceover("Let's review how to multiply fractions. This is a common operation in math and is useful in various real-life situations, like cooking.")
]
#only("2")[
#voiceover("For example, if a recipe calls for two-thirds of a cup of sugar, and you want to make half the recipe, you need to multiply two-thirds by one-half.")
]
#only("3")[
#voiceover("Let's go over the steps to multiply fractions.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Steps to Multiply Fractions 🧩]
#v(40pt)
#only("1-")[- Step 1: Multiply the numerators. 🔢]
#only("2-")[- Step 2: Multiply the denominators. 🔢]
#only("3-")[- Step 3: Simplify the fraction if possible. ✂️]
#only("1")[
#voiceover("Step 1: Multiply the numerators, which are the top numbers of the fractions.")
]
#only("2")[
#voiceover("Step 2: Multiply the denominators, which are the bottom numbers of the fractions.")
]
#only("3")[
#voiceover("Step 3: Simplify the fraction if possible.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Multiply 2/3 and 1/2 🧮]
#v(40pt)
#only("1-")[$frac(2,3) dot frac(1,2)$]
#only("2-")[$= frac(2 dot 1, 3 dot 2)$]
#only("3-")[$= frac(2,6)$]
#only("4-")[$= frac(1,3)$]
#only("1")[
#voiceover("Let's multiply two-thirds by one-half.")
]
#only("2")[
#voiceover("First, multiply the numerators: two times one.")
]
#only("3")[
#voiceover("Next, multiply the denominators: three times two.")
]
#only("4")[
#voiceover("Finally, simplify the fraction if possible. In this case, two-sixths simplifies to one-third.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary 📝]
#v(40pt)
#only("1-")[- Multiply the numerators together. 🔢]
#only("2-")[- Multiply the denominators together. 🔢]
#only("3-")[- Simplify the fraction if possible. ✂️]
#only("1")[
#voiceover("To summarize, to multiply fractions, first multiply the numerators together.")
]
#only("2")[
#voiceover("Then, multiply the denominators together.")
]
#only("3")[
#voiceover("Finally, simplify the fraction if possible.")
]
]