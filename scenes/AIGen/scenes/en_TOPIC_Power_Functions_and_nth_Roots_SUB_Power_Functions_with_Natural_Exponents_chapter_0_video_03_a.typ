#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Introduction to Power Functions with Natural Exponents]
#v(40pt)
#only("1-")[- $2^3$]
#only("1")[
#voiceover("Great job on answering the question! Let's go over the solution step-by-step.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Understanding $2^3$]
#v(40pt)
#only("1-")[- $2^3$ means $2$ multiplied by itself $3$ times.]
#only("1")[
#voiceover("First, let's understand what $2^3$ means. It means 2 multiplied by itself 3 times.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Calculation]
#v(40pt)
#only("1-")[- $2^3 = 2 * 2 * 2$]
#only("2-")[- $2 * 2 = 4$]
#only("3-")[- $4 * 2 = 8$]
#only("1")[
#voiceover("To calculate $2^3$, we start with $2 * 2 * 2$.")
]
#only("2")[
#voiceover("First, we multiply 2 by 2, which equals 4.")
]
#only("3")[
#voiceover("Then, we multiply 4 by 2, which equals 8.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Conclusion]
#v(40pt)
#only("1-")[- Therefore, $2^3 = 8$ 🎉]
#only("1")[
#voiceover("So, the value of $2^3$ is 8. Well done!")
]
]