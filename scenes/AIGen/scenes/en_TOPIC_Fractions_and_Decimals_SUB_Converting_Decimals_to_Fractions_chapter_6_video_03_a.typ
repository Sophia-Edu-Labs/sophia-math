#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#only("1")[
#voiceover("Great, you recognized that correctly! Let's go through the solution step by step together.")
]

#text(size: 30pt, weight: "bold")[Converting Decimals to Fractions]

#v(40pt)

#only("2-")[#text()[$0.44$]]
#only("2")[
#voiceover("We are given the decimal number zero point four four.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Steps to Convert Decimals to Fractions]
#v(40pt)
#only("1-")[- Step 1: Count the decimal places. 🔢]
#v(20pt)
#only("2-")[- Step 2: Remove the decimal point. ✂️]
#v(20pt)
#only("3-")[- Step 3: Divide by a power of ten. ➗]

#only("1")[
#voiceover("Before we solve the exercise, a quick reminder of the steps to convert decimals to fractions. First, count the number of decimal places in the decimal number.")
]
#only("2")[
#voiceover("Next, remove the decimal point from the number.")
]
#only("3")[
#voiceover("Then, divide the resulting number by a one followed by zeroes, where the number of zeroes equals the number of decimal places.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $0.44$ to a Fraction]
#v(40pt)
#only("1-")[- Step 1: 🔢 Count the decimal places in $0.44$. $arrow$ There are 2 decimal places.]
#v(20pt)
#only("2-")[- Step 2: ✂️ Remove the decimal point: $arrow 44$.]
#v(20pt)
#only("3-")[- Step 3: ➗ Divide by $100 arrow 44/100$.]

#only("1")[
#voiceover("Now let's apply these steps to the decimal number zero point four four. First, we count the decimal places. There are two decimal places.")
]
#only("2")[
#voiceover("Next, we remove the decimal point, which gives us forty-four.")
]
#only("3")[
#voiceover("Then, we divide forty-four by a one followed by two zeroes. Since zero point four four has two decimal places, we divide it by one hundred. So we get forty-four over one hundred.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Simplifying the Fraction]
#v(40pt)
#only("1-")[- Step 4: Simplify the fraction if possible. 🔄]
#v(20pt)
#only("2-")[$44/100 = 22/50$]
#v(20pt)
#only("3-")[$22/50 = 11/25$]

#only("1")[
#voiceover("Finally, we simplify the fraction if possible. In this case, we can simplify forty-four over one hundred.")
]
#only("2")[
#voiceover("We divide both the numerator and the denominator by two, which gives us twenty-two over fifty.")
]
#only("3")[
#voiceover("We can simplify further by dividing both the numerator and the denominator by two again, which gives us eleven over twenty-five. So, zero point four four is equal to eleven over twenty-five.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary]
#v(40pt)
#only("1-")[- $0.44 = 11/25$]
#v(20pt)
#only("2-")[- Count the decimal places. 🔢]
#v(20pt)
#only("3-")[- Remove the decimal point. ✂️]
#v(20pt)
#only("4-")[- Divide by a power of ten. ➗]
#v(20pt)
#only("5-")[- Simplify the fraction. 🔄]

#only("1")[
#voiceover("To summarize, zero point four four is equal to eleven over twenty-five.")
]
#only("2")[
#voiceover("Remember the steps: First, count the decimal places.")
]
#only("3")[
#voiceover("Second, remove the decimal point.")
]
#only("4")[
#voiceover("Third, divide by a power of ten.")
]
#only("5")[
#voiceover("And finally, simplify the fraction if possible.")
]
]