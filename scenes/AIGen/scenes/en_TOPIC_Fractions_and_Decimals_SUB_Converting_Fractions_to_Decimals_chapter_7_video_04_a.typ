#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#only("1")[
#voiceover("Great job! Let's go through the solution step by step together.")
]

#text(size: 30pt, weight: "bold")[Converting Complex Fractions to Decimals]

#v(40pt)

#only("2-")[#text()[$3/5$]]
// The fraction 3/5 is shown from slide 2 onward

#only("2")[
#voiceover("We are given the fraction three-fifths and need to convert it to a decimal.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Steps to Convert Fractions to Decimals]
#v(40pt)
#only("1-")[- Step 1: Divide the numerator by the denominator. ➗]
#v(20pt)
#only("2-")[- Step 2: Simplify the result, if necessary. ✂️]
#only("1")[
#voiceover("Before we solve the exercise, let's quickly review the steps to convert fractions to decimals. First, divide the numerator by the denominator.")
]
#only("2")[
#voiceover("Next, simplify the result if necessary.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $3/5$ to a Decimal]
#v(40pt)
#only("1-")[- Step 1: ➗ Divide $3$ by $5$.]
#v(20pt)
#only("2-")[- Step 2: ✂️ Simplify the result: $0.6$.]

#only("1")[
#voiceover("Now let's apply these steps to the fraction three-fifths. First, we divide three by five.")
]
#only("2")[
#voiceover("The result is zero point six. And that's it, we're done! The solution is zero point six.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Conclusion]
#v(40pt)
#only("1-")[- The fraction $3/5$ is equal to the decimal $0.6$. 🎉]

#only("1")[
#voiceover("In conclusion, the fraction three-fifths is equal to the decimal zero point six. Well done!")
]
]