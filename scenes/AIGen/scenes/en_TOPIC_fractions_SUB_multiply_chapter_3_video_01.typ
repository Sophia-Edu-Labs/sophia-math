#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Introduction to Multiplying Fractions]
#v(40pt)
#only("1-")[- Multiplying fractions can involve whole numbers, mixed numbers, and improper fractions 🧮]
#v(20pt)
#only("2-")[- Let's explore how to multiply these different types of fractions together!]
#only("1")[
#voiceover("Welcome! Today, we will learn about multiplying fractions. This can involve whole numbers, mixed numbers, and improper fractions. Let's dive in!")
]
#only("2")[
#voiceover("Let's explore how to multiply these different types of fractions together!")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Guide]
#v(40pt)
#only("1-")[- Whole numbers as fractions: $3 = 3/1$]
#v(20pt)
#only("2-")[- Mixed numbers to improper fractions: $2 1/2 = 5/2$]
#v(20pt)
#only("3-")[- Multiplying fractions: $a/b dot c/d = (a dot c)/(b dot d)$]
#v(20pt)
#only("4-")[- Simplify the result: $6/8 = 3/4$]
#only("1")[
#voiceover("First, let's convert whole numbers to fractions. For example, the whole number 3 can be written as 3 over 1.")
]
#only("2")[
#voiceover("Next, let's convert mixed numbers to improper fractions. For example, 2 and a half can be written as 5 over 2.")
]
#only("3")[
#voiceover("Now, let's multiply fractions. To multiply fractions, multiply the numerators together and the denominators together. For example, a over b times c over d equals a times c over b times d.")
]
#only("4")[
#voiceover("Finally, simplify the result if possible. For example, 6 over 8 simplifies to 3 over 4.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Recap]
#v(40pt)
#only("1-")[- Convert whole numbers to fractions: $3 = 3/1$]
#v(20pt)
#only("2-")[- Convert mixed numbers to improper fractions: $2 1/2 = 5/2$]
#v(20pt)
#only("3-")[- Multiply fractions: $a/b dot c/d = (a dot c)/(b dot d)$]
#v(20pt)
#only("4-")[- Simplify the result: $6/8 = 3/4$]
#only("1")[
#voiceover("To recap, first convert whole numbers to fractions, like 3 to 3 over 1. Next, convert mixed numbers to improper fractions, like 2 and a half to 5 over 2. Then, multiply the fractions by multiplying the numerators and denominators. Finally, simplify the result if possible, like 6 over 8 to 3 over 4.")
]
]