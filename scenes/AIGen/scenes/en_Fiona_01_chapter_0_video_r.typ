#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Review: Absolute and Relative Frequency 🎲]
#v(40pt)
#only("1-")[- Absolute frequency: counts occurrences of an event 📊]
#only("2-")[- Relative frequency: measures how often an event occurs relative to total trials 📈]
#only("3-")[- Let's review these concepts with an example of dice rolls 🎲]
#only("1")[
#voiceover("Let's review the concepts of absolute and relative frequency. Absolute frequency counts the occurrences of an event.")
]
#only("2")[
#voiceover("Relative frequency measures how often an event occurs relative to the total number of trials.")
]
#only("3")[
#voiceover("Let's review these concepts with an example of dice rolls.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Dice Rolls Example 🎲]
#v(40pt)
#only("1-")[- Roll a fair six-sided die 10 times 🎲]
#only("2-")[- Record the results: $3, 4, 2, 6, 3, 5, 1, 3, 4, 2$]
#only("3-")[- Absolute frequency of rolling a 3: $3$ times]
#only("4-")[- Relative frequency of rolling a 3: $3/10 = 0.3$]
#only("1")[
#voiceover("Consider rolling a fair six-sided die 10 times.")
]
#only("2")[
#voiceover("Record the results: three, four, two, six, three, five, one, three, four, two.")
]
#only("3")[
#voiceover("The absolute frequency of rolling a three is three times.")
]
#only("4")[
#voiceover("The relative frequency of rolling a three is three out of ten, which equals zero point three.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary 📝]
#v(40pt)
#only("1-")[- Absolute frequency: counts occurrences 📊]
#only("2-")[- Relative frequency: measures occurrences relative to total trials 📈]
#only("3-")[- Example: rolling a 3 three times out of 10 rolls 🎲]
#only("4-")[- Relative frequency of rolling a 3: $3/10 = 0.3$]
#only("1")[
#voiceover("To summarize, absolute frequency counts the occurrences of an event.")
]
#only("2")[
#voiceover("Relative frequency measures how often an event occurs relative to the total number of trials.")
]
#only("3")[
#voiceover("For example, rolling a three three times out of ten rolls.")
]
#only("4")[
#voiceover("The relative frequency of rolling a three is three out of ten, which equals zero point three.")
]
]