#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Review: Converting Fractions to Decimals]
#v(40pt)
#only("1-")[- Fractions with denominators of 10, 100, or 1000. 🔢]
#only("2-")[- Convert to decimals by placing the decimal point. ➗]
#only("3-")[- Useful for simplifying calculations and understanding numbers. 🔍]
#only("1")[
#voiceover("Let's review how to convert fractions with denominators of 10, 100, or 1000 to decimals. These fractions are common in many contexts.")
]
#only("2")[
#voiceover("The key is to convert these fractions to decimals by placing the decimal point correctly.")
]
#only("3")[
#voiceover("This method is useful for simplifying calculations and understanding numbers better.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Conversion]
#v(40pt)
#only("1-")[- Step 1: Identify the denominator. 🧮]
#v(20pt)
#only("2-")[- Step 2: Write the numerator as a number. ✍️]
#v(20pt)
#only("3-")[- Step 3: Place the decimal point according to the number of zeros in the denominator. ➕]
#v(20pt)
#only("4-")[- Example: $7/10$ becomes $0.7$.]
#only("1")[
#voiceover("Here are the steps to convert fractions to decimals.")
]
#only("2")[
#voiceover("First, identify the denominator.")
]
#only("3")[
#voiceover("Next, write the numerator as a number.")
]
#only("4")[
#voiceover("Finally, place the decimal point according to the number of zeros in the denominator. For example, seven over ten becomes zero point seven.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $75/100$ to a Decimal]
#v(40pt)
#only("1-")[- Step 1: Denominator is $100$.]
#v(20pt)
#only("2-")[- Step 2: Numerator is $75$.]
#v(20pt)
#only("3-")[- Step 3: Place the decimal point: $0.75$.]
#only("1")[
#voiceover("Let's go through an example. To convert seventy-five over one hundred to a decimal, first identify the denominator as one hundred.")
]
#only("2")[
#voiceover("Next, write the numerator, which is seventy-five, as a regular number.")
]
#only("3")[
#voiceover("Finally, place the decimal point two places to the left, giving us zero point seventy-five.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary]
#v(40pt)
#only("1-")[- Identify the denominator. 🧮]
#v(20pt)
#only("2-")[- Write the numerator as a number. ✍️]
#v(20pt)
#only("3-")[- Place the decimal point according to the number of zeros in the denominator. ➕]
#only("1")[
#voiceover("To summarize, first identify the denominator.")
]
#only("2")[
#voiceover("Then, write the numerator as a number.")
]
#only("3")[
#voiceover("Finally, place the decimal point according to the number of zeros in the denominator. That's how you convert fractions with denominators of 10, 100, or 1000 to decimals!")
]
]