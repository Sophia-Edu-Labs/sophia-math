#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Compensated Demand Function]
#v(40pt)
#only("1-")[The compensated demand function for good X is:]
#v(20pt)
#only("2-")[$h_X(p_X, p_Y, U)$]
#v(20pt)
#only("3-")[where:]
#v(10pt)
#only("4-")[- $p_X$ is the price of good X 💰]
#v(10pt)
#only("5-")[- $p_Y$ is the price of good Y 💰]
#v(10pt)  
#only("6-")[- $U$ is the utility level 😊]

#only("1")[
#voiceover("Not quite. Let's review the correct equation for the compensated demand function for good X.")
]
#only("2")[
#voiceover("The compensated demand function for good X is denoted as h sub X and is a function of three variables.")
]
#only("3")[
#voiceover("Let's break down what each variable represents.")
]
#only("4")[
#voiceover("p sub X represents the price of good X.")
]
#only("5")[
#voiceover("p sub Y represents the price of good Y.")
]
#only("6")[
#voiceover("And U represents the utility level that is being held constant in the Hicks Decomposition.")
]
]

#slide()[
#text(size: 30pt, weight: "bold")[Interpreting the Function]
#v(40pt)
#only("1-")[- $h_X(p_X, p_Y, U)$ gives the quantity of good X demanded 📈]
#v(20pt)
#only("2-")[- At prices $p_X$ and $p_Y$ 💰]
#v(20pt)
#only("3-")[- While maintaining utility level $U$ 😊]

#only("1")[
#voiceover("The compensated demand function h sub X of p sub X, p sub Y, and U gives the quantity of good X demanded")
]
#only("2")[
#voiceover("at prices p sub X and p sub Y")
]
#only("3")[
#voiceover("while maintaining the utility level U.")
]
]

#slide()[
#text(size: 30pt, weight: "bold")[Key Points]
#v(40pt)
#only("1-")[- Compensated demand holds utility constant 📊]
#v(20pt)
#only("2-")[- Allows isolation of substitution effect 🔍]
#v(20pt)
#only("3-")[- Key component of Hicks Decomposition 🔑]

#only("1")[
#voiceover("Remember, the key feature of compensated demand is that it holds utility constant.")
]
#only("2")[
#voiceover("This allows us to isolate the substitution effect of a price change.")
]
#only("3")[
#voiceover("The compensated demand function is a crucial component of the Hicks Decomposition, which separates the effect of a price change into substitution and income effects.")
]
]