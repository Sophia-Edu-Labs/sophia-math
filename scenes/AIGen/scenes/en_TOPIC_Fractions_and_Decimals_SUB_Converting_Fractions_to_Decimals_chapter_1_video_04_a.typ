#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#only("1")[
#voiceover("Great, you recognized that correctly! Let's go through the solution step by step together.")
]

#text(size: 30pt, weight: "bold")[Converting Fractions to Decimals]

#v(40pt)

#only("2-")[#text()[$12/100$]]
// The fraction 12/100 is shown from slide 2 onward

#only("2")[
#voiceover("So we're considering the fraction twelve over one hundred.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Steps to Convert Fractions to Decimals]
#v(40pt)
#only("1-")[- Step 1: Identify the fraction. 🧮]
#v(20pt)
#only("2-")[- Step 2: Divide the numerator by the denominator. ➗]
#v(20pt)
#only("3-")[- Step 3: Write the result as a decimal. ✍️]

#only("1")[
#voiceover("Before we solve the exercise, a quick reminder of the steps to convert fractions to decimals. First, identify the fraction.")
]
#only("2")[
#voiceover("Next, divide the numerator by the denominator.")
]
#only("3")[
#voiceover("Finally, write the result as a decimal.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $12/100$ to a Decimal]
#v(40pt)
#only("1-")[- Step 1: Identify the fraction $12/100$.]
#v(20pt)
#only("2-")[- Step 2: Divide $12$ by $100$. ➗]
#v(20pt)
#only("3-")[- Step 3: Write the result as $0.12$. ✍️]

#only("1")[
#voiceover("Now let's apply these steps to the fraction twelve over one hundred. First, we identify the fraction, which is twelve over one hundred.")
]
#only("2")[
#voiceover("Next, we divide twelve by one hundred. Twelve divided by one hundred equals zero point one two.")
]
#only("3")[
#voiceover("Finally, we write the result as a decimal. So, twelve over one hundred is equal to zero point one two. And that's it, we're done!")
]
]