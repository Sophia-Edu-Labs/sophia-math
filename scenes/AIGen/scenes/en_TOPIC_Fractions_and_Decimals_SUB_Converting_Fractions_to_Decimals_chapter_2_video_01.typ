#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Introduction to Converting Fractions to Decimals]
#v(40pt)
#only("1-")[- Imagine you have a fraction like $7/10$.]
#v(20pt)
#only("2-")[- How can we write this as a decimal number?]
#only("1")[
#voiceover("Let's start by understanding how to convert fractions to decimals. Imagine you have a fraction like seven over ten.")
]
#only("2")[
#voiceover("How can we write this fraction as a decimal number? Let's find out!")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Conversion]
#v(40pt)
#only("1-")[- Step 1: Identify the denominator. 🧮]
#v(20pt)
#only("2-")[- Step 2: Write the numerator as a number. ✍️]
#v(20pt)
#only("3-")[- Step 3: Add a decimal point before the last digit. ➕]
#v(20pt)
#only("4-")[- Example: $7/10$ becomes $0.7$.]
#only("1")[
#voiceover("I'll start by telling you three simple steps to do this.")
]
#only("2")[
#voiceover("First, we identify the denominator of the fraction.")
]
#only("3")[
#voiceover("Next, we write the numerator as a number.")
]
#only("4")[
#voiceover("Finally, we add a decimal point before the last digit of the numerator.")
]
#only("5")[
#voiceover("For example, seven over ten becomes zero point seven.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $75/100$ to a Decimal]
#v(40pt)
#only("1-")[- Step 1: Denominator is $100$.]
#v(20pt)
#only("2-")[- Step 2: Numerator is $75$.]
#v(20pt)
#only("3-")[- Step 3: Add a decimal point before the last two digits: $0.75$.]
#only("1")[
#voiceover("Let's go through an example with a bit more detail. To convert seventy-five over one hundred to a decimal, we first note that the denominator is one hundred.")
]
#only("2")[
#voiceover("Next, we write the numerator, which is seventy-five, as a regular number.")
]
#only("3")[
#voiceover("Finally, we add a decimal point before the last two digits of the numerator, giving us point seventy-five, which is typically written as zero point seventy-five.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary]
#v(40pt)
#only("1-")[- Identify the denominator. 🧮]
#v(20pt)
#only("2-")[- Write the numerator as a number. ✍️]
#v(20pt)
#only("3-")[- Add a decimal point before the last digit(s). ➕]
#only("1")[
#voiceover("To summarize, first identify the denominator of the fraction.")
]
#only("2")[
#voiceover("Then, write the numerator as a number.")
]
#only("3")[
#voiceover("Finally, add a decimal point before the last digit or digits of the numerator, depending on the number of zeros in the denominator. And that's how you convert fractions with ten, one hundred, or one thousand as the denominator to decimals!")
]
]