#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]

#slide()[
#text(size: 30pt, weight: "bold")[Kompensierte Nachfragefunktion]
#v(40pt)
#only("1-")[Die kompensierte Nachfragefunktion für Gut X lautet:]
#v(20pt)
#only("2-")[$h_X(p_X, p_Y, U)$]
#v(20pt)
#only("3-")[wobei:]
#v(10pt)
#only("4-")[- $p_X$ der Preis von Gut X ist 💰]
#v(10pt)
#only("5-")[- $p_Y$ der Preis von Gut Y ist 💰]
#v(10pt)  
#only("6-")[- $U$ das Nutzenniveau ist 😊]

#only("1")[
#voiceover("Leider nicht ganz richtig. Hier ist die richtige Gleichung für die kompensierte Nachfragefunktion für Gut X.")
]
#only("2")[
#voiceover("Die kompensierte Nachfragefunktion für Gut X wird als h Index X bezeichnet und ist eine Funktion von drei Variablen.")
]
#only("3")[
#voiceover("Lass uns aufschlüsseln, was jede Variable bedeutet.")
]
#only("4")[
#voiceover("p Index X steht für den Preis von Gut X.")
]
#only("5")[
#voiceover("p Index Y steht für den Preis von Gut Y.")
]
#only("6")[
#voiceover("Und U steht für das Nutzenniveau, das im Hicks-Dekompositionsverfahren konstant gehalten wird.")
]
]

#slide()[
#text(size: 30pt, weight: "bold")[Interpretation der Funktion]
#v(40pt)
#only("1-")[- $h_X(p_X, p_Y, U)$ gibt die nachgefragte Menge von Gut X an 📈]
#v(20pt)
#only("2-")[- Zu den Preisen $p_X$ und $p_Y$ 💰]
#v(20pt)
#only("3-")[- Während das Nutzenniveau $U$ beibehalten wird 😊]

#only("1")[
#voiceover("Die kompensierte Nachfragefunktion h Index X von p Index X, p Index Y und U gibt die nachgefragte Menge von Gut X an")
]
#only("2")[
#voiceover("zu den Preisen p Index X und p Index Y")
]
#only("3")[
#voiceover("während das Nutzenniveau U beibehalten wird.")
]
]

#slide()[
#text(size: 30pt, weight: "bold")[Wichtige Punkte]
#v(40pt)
#only("1-")[- Kompensierte Nachfrage hält das Nutzenniveau konstant 📊]
#v(20pt)
#only("2-")[- Ermöglicht die Isolierung des Substitutionseffekts 🔍]
#v(20pt)
#only("3-")[- Wichtiger Bestandteil der Hicks-Dekomposition 🔑]

#only("1")[
#voiceover("Denke daran, dass das Hauptmerkmal der kompensierten Nachfrage darin besteht, dass sie das Nutzenniveau konstant hält.")
]
#only("2")[
#voiceover("Dies ermöglicht es uns, den Substitutionseffekt einer Preisänderung zu isolieren.")
]
#only("3")[
#voiceover("Die kompensierte Nachfragefunktion ist ein wesentlicher Bestandteil der Hicks-Dekomposition, die den Effekt einer Preisänderung in Substitutions- und Einkommenseffekte aufteilt.")
]
]