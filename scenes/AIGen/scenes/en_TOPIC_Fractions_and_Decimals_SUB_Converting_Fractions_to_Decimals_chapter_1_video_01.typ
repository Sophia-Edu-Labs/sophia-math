#import "../sophiatheme.typ": *
#show: sophia-theme
#show figure.caption: it => [
#set text(size: 20pt)
#it.body
]


#slide()[
#text(size: 30pt, weight: "bold")[Introduction to Converting Fractions to Decimals]
#v(40pt)
#only("1-")[- Imagine you have a fraction like $75/100$.]
#v(20pt)
#only("2-")[- How can we write this as a decimal number?]
#only("1")[
#voiceover("Let's find out how to convert fractions to decimals. We'll start with the special case of fractions that have one hundred as the denominator. For example, seventy-five over one hundred.")
]
#only("2")[
#voiceover("How can we write this fraction as a decimal number? Let's find out!")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Step-by-Step Conversion]
#v(40pt)
#only("2-")[- Step 1: Identify the denominator. 🧮]
#v(20pt)
#only("3-")[- Step 2: Write the numerator as a number. ✍️]
#v(20pt)
#only("4-")[- Step 3: Add a decimal point before the second to last digit. ➕]
#v(20pt)
#only("5-")[- Example: $75/100$ becomes $0.75$.]
#only("1")[
#voiceover("I'll start by telling you three simple steps to do this.")
]
#only("2")[
#voiceover("First, we identify the denominator of the fraction, which is one hundred.")
]
#only("3")[
#voiceover("Next, we write the numerator, which is seventy-five, as a number.")
]
#only("4")[
#voiceover("Finally, we add a decimal point before the second to last digit of the numerator.")
]
#only("5")[
#voiceover("For example, seventy-five over one hundred becomes zero point seventy-five.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Example: Converting $45/100$ to a Decimal]
#v(40pt)
#only("1-")[- Step 1: Denominator is $100$.]
#v(20pt)
#only("2-")[- Step 2: Numerator is $45$.]
#v(20pt)
#only("3-")[- Step 3: Add a decimal point before the second to last digit: $0.45$.]
#only("1")[
#voiceover("Let's go through an example with a bit more detail. To convert forty-five over one hundred to a decimal, we first note that the denominator is one hundred.")
]
#only("2")[
#voiceover("Next, we write the numerator, which is forty-five, as a regular number.")
]
#only("3")[
#voiceover("Finally, we add a decimal point before the second to last digit of the numerator, giving us zero point forty-five.")
]
]


#slide()[
#text(size: 30pt, weight: "bold")[Summary]
#v(40pt)
#only("1-")[- Identify the denominator. 🧮]
#v(20pt)
#only("2-")[- Write the numerator as a number. ✍️]
#v(20pt)
#only("3-")[- Add a decimal point before the second to last digit. ➕]
#only("1")[
#voiceover("To summarize, first identify the denominator of the fraction, which is one hundred.")
]
#only("2")[
#voiceover("Then, write the numerator as a number.")
]
#only("3")[
#voiceover("Finally, add a decimal point before the second to last digit of the numerator. And that's how you convert fractions with one hundred as the denominator to decimals!")
]
]